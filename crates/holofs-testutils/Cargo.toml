[package]
name = "holofs-testutils"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Shared test-only utilities: mock wire-protocol node, env-guard for HOLOFS_POOL_DISABLE. Consumed via dev-dependencies."

[dependencies]
holofs-wire.workspace = true
tokio.workspace = true

[lints]
workspace = true
